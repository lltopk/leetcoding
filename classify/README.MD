https://github.com/EndlessCheng

https://oi-wiki.org/

## 复杂度评估
数组长度10^3 限制O(n^2)

数组长度10^5 限制O(nlogn)

数组长度10^7 限制O(n)

数组长度10^9 限制O(logn)

## 数学
### 取整
向下取整: a/b

向上取整: (a+b-1)/b

### 真除
除法真实值: a/(b*1.0) 或者 (double)a/b, 注意不可double(a/b), 这样会提前丢失数位

### 数列
等差数列`a[0] + a[1] + a[2] + ... + a[n-1]`, 求和公式`Sn = n(a[0]+na[n-1])/2`，假设公差为`d`, 也可以表示为`Sn = na[0]+n(n-1)d/2`

### 求根
一元二次方程`ax^2+bx+c = 0`, 求根公式`( -b - sqrt(b^2 - 4ac) / 2a < x < ( -b + sqrt(b^2 - 4ac) / 2a`

特别的当一元二次方程由等差数列而来, 假设数列长度为x, d = a[0], 和为m
```
d + 2d + 3d + ... + xd = m

xd(1+x)/2 = m

x(1+x)/2 = m/d

x^2 +x -2m/d = 0
```
此时根`x=( - + sqrt(1 + 8m/d) / 2`

### 取模
根据带余除法定理，任意整数 `a` 都可以表示为 `a=qm+r (m!=0)`，其中整数 `q` 为 `a` 除以 `m` 的商（quotient），整数 `r` 为 `a` 除以 `m` 的余数（remainder），即 `r = a mod m`

**加法乘法取模**, 可根据带余除法定理自行证明
```txt
(a+b) % m = ((a % m) + (b % m)) % m
(a*b) % m=((a % m) * (b % m)) % m
```

TODO 负数减法取模

TODO 除法取模

### 快速幂
如何计算`x`的`n`次幂`pow(x,n)`, 快速幂推导如下

将n用二进制表示, 例如求`pow(x, 13)`, `13`表示为二进制是`1101`

计算过程为, 从低到高枚举二进制的1位, 并以该位代表的实际二进制值作为幂, 累乘即x * x^4 * x^8
```java
    public double myPow(double x, int N) {
        double ans = 1;
        long n = N;
        //x^(-n) = (1/x)^n
        if(n<0){
            x = 1/x;
            n = -n;
        }

        //快速幂, 将n用二进制表示, 例如求pow(x, 13), 13表示为二进制是1101
        //计算过程为, 从低到高枚举二进制的1位, 并以该位代表的实际二进制值作为幂, 累乘即x * x^4 * x^8
        while(n!=0){
            if((n & 1) == 1){
                ans *= x; 
            }
            //以任意位代表的实际二进制值作为幂, 如1, 2, 4, 8
            x *=x;
            n >>= 1;
        }
        return ans;
    }
```

## 位运算