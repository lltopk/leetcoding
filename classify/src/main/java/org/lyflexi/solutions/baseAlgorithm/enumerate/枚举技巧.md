
## 双变量问题 枚举右维护左
_对于 双变量问题，例如两数之和 `nums[i] + nums[j] == target`_

可以枚举右边的`nums[r]`, 这可以转换成 单变量问题，也就是在 nums[r]左边查找是否有`nums[l] = target - nums[r]`

上述枚举过程可能需要辅助变量`val`, 辅助哈希表`HashMap`, 或者当字典大小确定的时候用辅助数组`int[]/int[][]`维护。 

我把这个技巧叫做 **枚举右，维护左**. 具体可以拆分为两类场景
- 当右是影响因子, 我们要后更新辅助结构, 这类场景辅助结构用于存储计数值
- 当左是影响因子, 我们要先更新辅助结构, 这类场景辅助结构用于存储滚动值

### 辅助结构存储计数值, 后更新辅助结构
_辅助结构用于存储计数值, 这类场景要先更新答案后更新辅助结构，辅助结构在任意计算轮次中都不应该包含当前枚举右, 否则会导致额外错误的计算_

例如LC624. 数组列表中的最大距离, 利用辅助变量
```java
    public int maxDistance(List<List<Integer>> arrays) {
    //怎么利用各个数组有序的条件呢？  可以直接O(1)访问每个数组的第一个元素为局部最小值， 每个数组的最后一个元素为局部最大值
    int ans = 0;
    int preMin = Integer.MAX_VALUE/2;//防止减法溢出
    int preMax = Integer.MIN_VALUE/2;//题目元素中存在负数
    int n = arrays.size();
    for(int r = 0; r< n; r++){
        List<Integer> list = arrays.get(r);
        //假设遍历到右端点, 当前最大差有两种算法
        //1. preMax - list.get(0)
        //2. list.get(list.size()-1) - preMin
        ans = Math.max(ans, Math.max(preMax - list.get(0), list.get(list.size()-1) - preMin));
        preMin = Math.min(preMin, list.get(0));
        preMax = Math.max(preMax, list.get(list.size() - 1));
    }

    //不能最后return preMax - preMin, 因为有可能最大值和最小值出现在同一个数组， 不符合题意
    return ans;
}
```

例如LC1512. 好数对的数目, 利用辅助哈希表
```java
    public int numIdenticalPairs(int[] nums) {
        int ans = 0, n = nums.length;
        //记录元素出现的次数
        Map<Integer, Integer> map = new HashMap<>();
        for(int r = 0; r<n; r++){
            if(!(map.get(nums[r]) == null)){
                //右端点是影响因子, 每出现一个有效右端点, 好数对的增加恰好等于元素过去出现的次数
                ans+=map.get(nums[r]);
            }
            map.merge(nums[r], 1, Integer::sum);
        }

        return ans;
    }
```

例如2342. 数位和相等数对的最大和, 利用辅助哈希表
```java
    public int maximumSum(int[] nums) {
        int n = nums.length, ans = -1;
        //数位和最多为9个9
        int[] dict = new int[82];//统计数位和 对应 实际元素最大值
        //初始化MIN_VALUE
        for(int s = 0; s <=81; s++){
            dict[s] = Integer.MIN_VALUE;
        }
        for(int r = 0; r< n; r++){
            int sumBit = 0, num = nums[r];
            while(num!=0){
                sumBit+=num%10;
                num/=10;
            }
            //由于初始化了MIN_VALUE， 因此若dict[sumBit]不存在， 恰好也满足ans为-1
            ans = Math.max(ans, nums[r] + dict[sumBit]);
            //维护左
            dict[sumBit] = Math.max(dict[sumBit], nums[r]);
        }

        return ans;
    }
```

### 辅助结构存储滚动值, 先更新辅助结构
_滚动计算的辅助结构含义是, 以`l`为首的前缀元素的最大值`mx`或者最小值`mn`,_ 

`l`怎么计算?

这类场景中辅助结构取决于当前的右端点, 比如`l = r - mid / l = r - mid +1`, 因此要先更新辅助结构后更新答案
```java
mx = f(l)
mn = f(l)
```

这类题目有3584.子序列首尾元素的最大乘积, 维护最大值`mx`/最小值`mn`
```java
    public long maximumProduct(int[] nums, int m) {
        //最左侧的元素为r - (m - 1), 注意由于是子序列而不是子数组, 所以该位置是最小约束, 左界不能在该位置的右侧
        //滚动求该位置的最大值和最小值
        int mn = Integer.MAX_VALUE, mx = Integer.MIN_VALUE;
        long ans = Long.MIN_VALUE;//答案有可能是负值
        int n = nums.length;
        //r从m-1开始, 保证r-(m-1)会从0开始
        for(int r = m-1; r< n; r++){
            int i = r - m + 1;
            mn = Math.min(mn, nums[i]);
            mx = Math.max(mx, nums[i]);
            long y = (long)nums[r];//防止下面乘法越界
            ans = Math.max(ans, Math.max(mn*y, mx*y));
        }
        return ans;
    }
```

2905.找出满足差值条件的下标 II, 维护最大值索引`mxIdx`/最小值索引`mxIdx`
```java
    public int[] findIndices(int[] nums, int indexDifference, int valueDifference) {
        int maxIdx = 0;
        int minIdx = 0;
        for (int j = indexDifference; j < nums.length; j++) {
            //枚举右j, 维护左: j - indexDifference
            int i = j - indexDifference;
            if (nums[i] > nums[maxIdx]) {
                maxIdx = i;
            } else if (nums[i] < nums[minIdx]) {
                minIdx = i;
            }
            //abs(nums[i] - nums[j]) >= valueDifference等价于以下两个场景

            //1. |nums[j] - nums[minIdx]|
            if (Math.abs(nums[j] - nums[minIdx]) >= valueDifference) {
                return new int[]{minIdx, j};
            }
            //2. |nums[maxIdx] - nums[j]|, 如[5,1,4,1], 答案为[0,3]
            if (Math.abs(nums[maxIdx] - nums[j]) >= valueDifference) {
                return new int[]{maxIdx, j};
            }

        }
        return new int[]{-1, -1};
    }
```

## 三变量问题 枚举中间 维护后缀状态集
_对于有三个或者四个变量的问题，其下标满足 `0≤i<j<k<n`, 往往枚举中间`j`更好算。_

此时 `i` 和 `k` 自动被 `j` 隔开，互相独立，后续计算中无需关心 `i` 和 `k` 的位置关系。

这类题目需要两个辅助结构, 前缀辅助`pre`, 后缀辅助`suf`
- `pre`往往可以滚动计算
- `suf`往往是个辅助数组`suf[]`

其中`suf[]`需要预计算, 用整个数组做初始化

### 预初始化后缀状态集
用`suf[j+1]`表示当前枚举`j`的后缀某维度的统计状态, `suf[j+1]`是个动态数组受枚举的具体位置`j`影响, 所以各个枚举`j`状态下的`suf[j+1]`值都不同

例如2908.元素和最小的山形三元组
```java
    public int minimumSum(int[] nums) {
    int ans = Integer.MAX_VALUE, n = nums.length;

    int preMin = Integer.MAX_VALUE;//滚动计算
    int sufMin[] = new int[n];//代表nums[j]的后缀最小值, 包括nums[j]在内, 是个动态的数组
    //预计算 j: [2, n-1]
    sufMin[n-1] = nums[n-1];
    //后缀最小公式: sufMin[j] = Math.min(nums[j], sufMin[j+1]);
    for(int j = n-2; j>0; j--){
        sufMin[j] = Math.min(nums[j], sufMin[j+1]);
    }

    //三元素, 枚举中间
    for(int j = 0; j< n-1; j++){

        if(nums[j] > preMin && nums[j] > sufMin[j+1]){
            ans = Math.min(ans, preMin+nums[j]+sufMin[j+1]);
        }
        preMin = Math.min(preMin, nums[j]);
    }

    return ans==Integer.MAX_VALUE?-1:ans;
}
```

### 正式遍历回撤更新后缀状态集
用`suf[]`表示当前枚举位置`j`之后的任意元素的个数, 用于计数

这类场景下`suf[c]`依然是个动态数组, 由于受枚举的具体位置`j`影响, 所以要在正式遍历数组的过程中做计数上的撤销

如1930. 长度为 3 的不同回文子序列
```java
    //这道题虽说求个数, 但暗含着去重
public int countPalindromicSubsequence(String s) {
    char[] sArray = s.toCharArray();
    int n = sArray.length, ans = 0;
    //去重数组, 无需考虑c(i), 假设(c(j), c(k))为一对回文, 用于去重
    boolean[][] has = new boolean[26][26];

    boolean[] pre = new boolean[26];//优化前缀辅助结构, 表示当前索引j之前的任意元素c是否存在, 无需统计个数

    int[] suf = new int[26];//后缀辅助结构, 表示当前索引j之后的任意元素c存在的个数, 后缀只能用个数表示元素是否存在

    for(char c: sArray){
        suf[c - 'a']++;
    }

    for(int j = 0; j<n; j++){
        char c = sArray[j];
        //撤销
        suf[c-'a']--;
        //匹配26个任意字符
        for(int i = 0; i< 26; i++){
            if(suf[i]>0 && pre[i] && !has[c-'a'][i]){
                has[c-'a'][i] = true;
                ans++;
            }
        }
        //滚动
        pre[c-'a'] = true;
    }

    return ans;
}
```
