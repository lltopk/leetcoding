双指针 `Two Point`

- 定长滑动窗口
- 变长滑动窗口
- 同向双指针
- 相向双指针
- 背向双指针
- 双序列双指针
- 三指针
- 分组循环

## 滑动窗口
在滑动窗口计算过程中, 我们习惯于枚举右端点`nums[r] r==i`, 此时窗口内连续子数组个数`[l,r], [l+1,r]...[r,r]`之和恰好等于窗口长度`r-l+1`

不要在内循环中遍历窗口计算, 这将会是O(nk)复杂度, 其中k为窗口大小. 滑动窗口的本质是计算入`compute(nums[i]){...}`以及计算出`compute(nums[l]){...l++;}`

## 双指针
