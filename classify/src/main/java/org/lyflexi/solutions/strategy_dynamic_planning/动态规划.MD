动态规划 `Dynamic Programing`

其实`DFS`与动态规划有很深的渊源, `DFS`根据子问题推导目标问题`P(N)`, 并且在实施层面有不同的策略
- 自顶向下递(类似回溯算法)，就是先序遍历，目标问题是底, 遍历一个点更新在此点上情况，并维护全局的情况；
- 自底向上归(类似动态规划)，就是后序遍历，目标问题是顶, 根据左子树的情况和右子树的情况，得到本树的情况。

## 转移方程

- 推导转移方程并填充 `dp[]`数组

